<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimary"
    android:orientation="vertical">

    <include layout="@layout/layout_title" />

    <android.support.design.widget.TabLayout
        android:id="@+id/tl_traffic_wisdom"
        android:layout_width="match_parent"
        android:layout_height="35dp"
        android:layout_gravity="center_horizontal"
        android:background="@color/colorPrimary"
        app:tabBackground="@drawable/tab_layout_select"
        app:tabGravity="center"
        app:tabIndicatorColor="@color/colorPrimary"
        app:tabMaxWidth="0dp"
        app:tabMode="fixed"
        app:tabPadding="@dimen/len_5"
        app:tabSelectedTextColor="@color/colorPrimary"
        app:tabTextAppearance="@style/tab_layout_text"
        app:tabTextColor="@color/yellow" />

    <android.support.v4.view.ViewPager
        android:id="@+id/vp_traffic_wisdom"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

</LinearLayout>
